# setupubuntu

## Full setup (one-shot)
```
./setup-ubuntu.sh
```

## Directory layout
- `parts/` — topic scripts orchestrated by `setup-ubuntu.sh` (also runnable individually)
- `topics/` — standalone topic scripts, run each one manually as needed